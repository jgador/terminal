<Window x:Class="AgentDock.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:term="clr-namespace:Microsoft.Terminal.Wpf;assembly=Microsoft.Terminal.Wpf"
        Title="AgentDock"
        Width="960"
        Height="640"
        MinWidth="640"
        MinHeight="420"
        Background="#111827"
        WindowStartupLocation="CenterScreen"
        mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Border Grid.Row="0"
                Padding="16,12"
                Background="#111827"
                BorderBrush="#2A3550"
                BorderThickness="0,0,0,1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <StackPanel Orientation="Vertical">
                    <TextBlock Text="AgentDock"
                               FontSize="18"
                               FontWeight="SemiBold"
                               Foreground="#F3F4F6" />
                    <TextBlock Text="Minimal WPF host for the terminal control"
                               Margin="0,2,0,0"
                               FontSize="12"
                               Foreground="#9CA3AF" />
                </StackPanel>

                <Border Grid.Column="1"
                        Padding="10,4"
                        Background="#1D4ED8"
                        VerticalAlignment="Center"
                        CornerRadius="999">
                    <TextBlock x:Name="SessionLabel"
                               Text="PowerShell"
                               FontSize="12"
                               FontWeight="SemiBold"
                               Foreground="White" />
                </Border>
            </Grid>
        </Border>

        <Border Grid.Row="1"
                Margin="12,12,12,12"
                BorderBrush="#2A3550"
                BorderThickness="1"
                Background="#0F172A">
            <term:TerminalControl x:Name="Terminal"
                                  Focusable="True" />
        </Border>
    </Grid>
</Window>
